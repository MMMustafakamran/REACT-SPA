# The page's own file, minus the secret:
#
#   ```dotenv title=".env"
#   OPENAI_API_KEY=sk-...
#   ```
#
# Copy this to `.env` beside server.ts, or export OPENAI_API_KEY in the
# terminal running it. `.env` is gitignored, as the page instructs.
OPENAI_API_KEY=sk-...
